# Railway Nixpacks configuration
[phases.setup]
nixPkgs = ["python311", "gcc", "gdal", "geos", "proj"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[start]
cmd = "uvicorn app.main:app --host 0.0.0.0 --port ${PORT:-8000}"
